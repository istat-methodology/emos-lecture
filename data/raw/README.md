# Raw Data

This folder contains original input data.

## `labelled_queries.csv`

Labelled queries with three columns:

- `query`: text query
- `n_words`: number of words in the query
- `true_code`: labelled true code, written in dotted ATECO style such as `73.11.0`

The file uses simple comma-separated columns so students can inspect it directly.
